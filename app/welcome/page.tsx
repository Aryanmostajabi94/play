import GlobeIntro from "../../components/onboarding/GlobeIntro";

// A1 — Globe Intro (+ A2 City Zoom Animation folded in as the same flow's
// final beat). See components/onboarding/GlobeIntro.tsx for the scope
// note on why this is a from-scratch build, not a bug fix.
export default function WelcomePage() {
  return <GlobeIntro />;
}
